# Audio files — drop these in locally, don't commit binaries
jeopardy/sounds/*.mp3
jeopardy/sounds/*.wav
jeopardy/sounds/*.ogg

# Game CSV/XLSX files — managed on the server, not in git
# (commit them if you want version control on your trivia sets)
# jeopardy/games/*.csv
# jeopardy/games/*.xlsx

# Node
node_modules/
npm-debug.log

# Editor
.vscode/
.DS_Store
